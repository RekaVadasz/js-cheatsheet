function functionName(parameter1, parameter2, parameter3) {
    // code to be executed
  }

/* 
parameters: the names listed in the function definition
    - data type is not specified for them
    - functions do not perform type checking on passed arguments, nor the number of arguments is checked


arguments: the real values passed to (and received by) the function.
    - if miSsing: value is undefined
    - we can give default values
*/

function myFunction(x, y = 2) { // y parameter is given 2 as default value
    // function code
  }

